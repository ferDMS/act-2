export interface User {
  id: string;
  name: string;
  count: number;
  color: string;
}

// You can add more user-related interfaces/types here
export interface UserCredentials {
  email: string;
  password: string;
}
